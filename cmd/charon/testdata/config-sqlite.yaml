server:
  listen: ":0"
charon:
  listen: ":0"
  base_url: ""
inference:
  base_url: "http://localhost:11434"
  timeout_seconds: 120
storage:
  backend: sqlite
  data_dir: ""  # overridden per-test
  sqlite:
    wal_mode: true
    busy_timeout_ms: 5000
  checkpoint_interval: 10
  ttl_days: 30
